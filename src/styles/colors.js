const error = '#ff4136'

const headerMain = '#d1609c'
const headerLight = '#e485b7'

const textDefault = '#232323'
const textMed = '#5d5656'
const textLight = '#727272'

export default {
  error,
  headerMain,
  headerLight,
  textDefault,
  textMed,
  textLight,
}
